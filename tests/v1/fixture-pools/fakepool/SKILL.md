# Skill: `foreman-pool-fakepool`

Test fixture, not a shipped pool. It stands in for any directory-added
pool: one fake vendor command, enough to launch a job end to end and to
exercise the packaged/user/broken three-deep fallback.
