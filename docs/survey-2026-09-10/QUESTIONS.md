You are being resumed once, by the Foreman (the orchestrator session, Fable 5.1), for a survey the owner ordered on 2026-09-10 before the v5 rebuild of Foreman starts. You were a supervisor, foreman or merge desk of a swarm on this machine. Answer from YOUR OWN experience on your front, concrete incidents over impressions, under 700 words total, plain prose with the five numbered headings below. Do not run jobs, do not edit files, do not launch anything; reading files to check a fact is fine. Write the answer as your final message.

The v5 design in one page (the owner's decisions of 2026-09-10): a front is started from the owner's inputs only (goal, one-sentence finish line, decisions, team as agent:effort:count:role, repositories with base/work/target branches). The supervisor itself reads the code and machine and writes a MAP (facts marked seen or assumed, refs resolved against the remote), then 5-8 MILESTONES (stable labels; changes allowed with a recorded reason), then grows a recursive TREE of task nodes down to job leaves, each node with a verify command, a must-not-touch line and a reason; one checking write door refuses malformed or misplaced nodes; append-only, folded on read. The worker's page is rendered by the runtime from the node plus map facts. Default instruction sheets per role, additions by the queuer, override only with a reason. Queues are 100% mechanical: the supervisor only queues, cancels, moves to front, edits unstarted; the runtime starts a job when its model has a free reserved slot, dependencies finished and team policy allows (backup builder only after a failure), and says why a job waits. The foreman's queue starts fronts in strict order when the whole team is reservable, stops/resumes, lands finished fronts onto their target, rebases open fronts automatically when the target moved (no front finishes while behind), relaunches dead supervisors, re-enables pools at reset, runs nightly checks, upgrades Foreman at safe points, cleans up, writes the owner report on a clock. Team quotas are held by the foreman and reserved exclusively per front; the foreman asks the owner with numbers to raise one. The merge desk agent is deleted; landing is a queued script at two levels (job onto front branch, front onto target: update onto remote head, per-repository check in a fresh worktree, push/PR per repository policy under a lock, everything recorded; failure returns as a finding). Per-repository policy. Fronts may span repositories. Screen rebuilt to show queues, reservations, milestones with pieces done and split count, the tree. Storage stays plain files through the one door. Observed-versus-declared untouched. The owner never approves a step; only finish-line changes, money, irreversible acts reach him. Cutover only after v5 runs a front on itself.

1. THE DESIGN. What is wrong, what is missing, what would have changed your own front. Name the clause.
2. THE TEAM. (a) The ideal team for a front like yours: roles, models, effort, how many at once, and the ceiling past which more workers hurt, with the incident that shows it. (b) How to CALIBRATE a team for ANY front: what to read off the goal, the map and the tree to decide counts and models (job independence, judgement versus mechanical work, number of repositories, risk), and which signals during a front say add a worker, remove one, or change model.
3. MODEL BEHAVIOUR. For each model you used (Muse, Grok 4.6, Opus 5, Astra 6, Sol, Fable 5.1, Sonnet): what it did well, how it failed, the spec shape that made it succeed, what you would forbid it. Concrete incidents.
4. WHAT COST YOU THE MOST TIME, one line each, and for each whether v5 fixes it or not.
5. WHAT YOU WOULD TELL YOUR SUCCESSOR that is written nowhere: the ruling you needed and never got, the thing you learned by failing.
